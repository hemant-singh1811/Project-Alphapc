// during the creation of table column name should be unique 
           >> if it duplicate -> display the message ;

// handle the special charcter during the creation of table

// make a sql quries editor where we write quries then result it show in box , 
                        if written quries have syntax error  
      |                      -> display the error(sql.message)
      |-> make a query editor with auto correct the names


// make a logout button    #done

//add feature where we add column or delete column